steps:
- name: 'gcr.io/cloud-builders/docker'
  args: [
            'build', 
            '--tag=gcr.io/$PROJECT_ID/udp-server:latest', 
            '--cache-from=gcr.io/$PROJECT_ID/udp-server:latest', 
            '-f', 'Dockerfile', 
            '.'
        ]
images: ['gcr.io/$PROJECT_ID/udp-server:latest']

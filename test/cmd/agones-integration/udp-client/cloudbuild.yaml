steps:
- name: 'gcr.io/cloud-builders/docker'
  args: [
            'build', 
            '--tag=gcr.io/$PROJECT_ID/udp-client:latest', 
            '--cache-from=gcr.io/$PROJECT_ID/udp-client:latest', 
            '-f', 'Dockerfile', 
            '.'
        ]
images: ['gcr.io/$PROJECT_ID/udp-client:latest']
